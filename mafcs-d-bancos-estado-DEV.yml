spring:
  data:
    mongodb:
      authentication-database: admin
      username: admin
      password: admin
      database: Bancos-DEV
      host: localhost
      port: 27017

controller:
  uri: /api/v1/bancos/estado/{state}

client:
  zipcode:
    name: Bancos-Json
    uri: /api/v1/legacy/bancos/estado/{estado}
